Full Stack Learning Repository (WIP)
Overview

This repository tracks my progression through full-stack development by building projects at different stages of learning.
Each project focuses on understanding core mechanics, system structure, and implementation detail, rather than production-level polish.

Current Projects
1. Cat Website (HTML + CSS Basics)

A foundational project focused on understanding basic HTML structure and CSS styling.

Core Concepts Practiced:

Semantic HTML structure (section, nav, form)
Linking external CSS
CSS selectors (class, id)
Basic styling (colors, fonts, layout)
Image embedding using external URLs
Forms and input elements

CSS Concepts:

Global styling via body
Class-based styling (.paragraph)
ID-based styling (#first-div)
Specificity and overrides (!important)

Purpose:

Understanding DOM structure
Understanding CSS application hierarchy
Building a mental model of browser rendering
2. Google Homepage Clone (Layout + UI Practice)

A more advanced static project replicating the layout and structure of the Google homepage.

Core Concepts Practiced:

Flexbox layout system
Responsive design using media queries
Navigation bar structuring
Input field styling
Button and icon systems
Footer layout

UI/UX Elements:

Search bar with icons
Hover and focus states
Circular icon buttons
Structured footer

Purpose:

Translating real-world UI into code
Mastering layout control and spacing
Understanding responsiveness constraints
3. Developer Portfolio (System Integration Layer)

A structured personal portfolio built using HTML and CSS, acting as a central aggregation layer for projects and identity representation.

📄 Source:
🎨 Styles:

Portfolio Breakdown
Structural Architecture

The portfolio is divided into modular sections:

Header → Identity, navigation, and external links
About → Contextual introduction layer
Experience → Timeline-based abstraction
Projects → Card-based project representation
Footer → Metadata and deployment information
Layout System
Mobile-first design approach
Grid-based structure scaling to multi-column layout
Sticky header behavior for context persistence
Section-based vertical segmentation
Component Design

Card System:

Reusable structure for experience and projects
Separation of content (text) and representation (image + metadata)

Navigation System:

Anchor-linked sections
Progressive disclosure (hidden nav on small screens)

Interaction Layer:

Hover-based feedback
Micro-animations (icon shifts, scaling)
Scroll-to-top utility
Styling System

Design Tokens:

--color-accent: #ff4d4d;
--color-highlight: #e2e8f8;

Key Patterns:

Consistent spacing using gap
Accent-driven interaction feedback
Dark theme with layered background (underlay)
Typography hierarchy using Inter font
Concepts Practiced
Component-based UI structuring
Responsive design systems
Separation of structure (HTML) and styling (CSS)
Reusability and modular layout design
Visual hierarchy and readability control
Purpose

This project acts as:

A convergence point of prior learning
A controlled system for UI architecture experimentation
A base layer for future full-stack expansion
Direction (WIP)

Future additions will include:

Short Term
JavaScript-based interactivity
DOM manipulation and event systems
Mid Term
Backend systems (Node.js, APIs)
Data-driven UI rendering
Long Term
Full-stack integrated applications
ML-driven interfaces and simulations
Approach
Build from scratch
Prioritize understanding over abstraction
Treat each project as a system, not a demo
Incrementally increase complexity
Status

Work in progress. Additional systems and deeper integrations will be added over time.
